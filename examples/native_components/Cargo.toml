[package]
name = "makepad-example-native-components"
version = "1.0.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2021"
description = "Makepad native components example - demonstrates embedding native UI components"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { path = "../../widgets", version = "1.0.0" }
makepad-native-components = { path = "../../makepad-native-components", version = "1.0.0" }

